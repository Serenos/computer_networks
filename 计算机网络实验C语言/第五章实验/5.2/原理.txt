配置文件中存放一个二维矩阵，表示网络拓扑中各个路由结点与其它结点之间的邻接关系。若两个结点直接相邻，则对应矩阵元素的值为两个结点之间的距离（正整数）。若两个结点之间不直接相邻，则对应矩阵元素的值为空（或99，表示无穷远）。
当前节点获得邻居节点的关系后，则可以运行djkstra算法来计算拓扑结构中该点到任意点的最短距离并存储到路由表中，程序中定义了两个数组，D数组代表其他点到该点的最短距离，P数组记录路径信息，searcgPath函数用来找到这条最短路径。